<!DOCTYPE html>
<html lang="en">
<head>
    <title>Todo List App</title>
    <style>
        body {
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            background: #f9fbfd;
            margin: 0;
            padding: 0;
            color: #333;
        }

        h1 {
            background: linear-gradient(135deg, #6dd5ed, #2193b0);
            color: white;
            margin: 0;
            padding: 30px;
            text-align: center;
            font-size: 30px;
            font-weight: bold;
            letter-spacing: 1px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }

        ul {
            list-style: none;
            padding: 20px;
            max-width: 700px;
            margin: 20px auto;
        }

        ul li {
            background: #ffffff;
            border-radius: 15px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            margin-bottom: 15px;
            padding: 18px 55px 18px 55px;
            position: relative;
            font-size: 18px;
            display: flex;
            align-items: center;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        ul li:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 18px rgba(0,0,0,0.15);
        }

        ul li::before {
            content: "📌";
            font-size: 20px;
            margin-right: 10px;
        }

        .delete-btn {
            color: #ff4d4d;
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            font-size: 18px;
            text-decoration: none;
            font-weight: bold;
        }

        .delete-btn:hover {
            color: #cc0000;
        }

        .edit-btn {
            color: #ffb400;
            position: absolute;
            right: 45px;
            top: 50%;
            transform: translateY(-50%);
            font-size: 18px;
            text-decoration: none;
            font-weight: bold;
        }

        .edit-btn:hover {
            color: #e69500;
        }

        form {
            background: #ffffff;
            padding: 25px;
            margin: 30px auto;
            max-width: 700px;
            border-radius: 15px;
            box-shadow: 0 6px 14px rgba(0,0,0,0.12);
            text-align: center;
        }

        label {
            display: block;
            font-size: 18px;
            margin-bottom: 10px;
            color: #444;
            font-weight: bold;
        }

        input[type="text"] {
            width: 80%;
            padding: 14px;
            margin-bottom: 15px;
            border: 2px solid #ddd;
            border-radius: 10px;
            font-size: 16px;
            outline: none;
            transition: 0.3s;
        }

        input[type="text"]:focus {
            border-color: #2193b0;
            box-shadow: 0 0 6px rgba(33,147,176,0.4);
        }

        input[type="submit"] {
            background: linear-gradient(135deg, #6dd5ed, #2193b0);
            color: white;
            border: none;
            padding: 14px 25px;
            border-radius: 10px;
            cursor: pointer;
            font-size: 16px;
            font-weight: bold;
            transition: 0.3s ease;
        }

        input[type="submit"]:hover {
            background: linear-gradient(135deg, #5ab9d6, #1b7a94);
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <h1>🌟 Welcome To Suchith's DevOps Session 🌟 <br> ✨ Write your plan on Learning Jenkins ✨</h1>

    <ul>
        <% todolist.forEach(function(todo, index) { %>
        <li>
            <a href="/todo/delete/<%= index %>" class="delete-btn">🗑</a>
            <a href="/todo/<%= index %>" class="edit-btn">✏</a>
            <%- todo %>
        </li>
        <% }); %>
    </ul>

    <form action="/todo/add/" method="post">
        <label for="newtodo">💡 Add Your Jenkins Learning Task</label>
        <input type="text" name="newtodo" id="newtodo" placeholder="e.g., Learn Jenkins Pipelines..." autofocus />
        <br>
        <input type="submit" value="➕ Add Task" />
    </form>
</body>
</html>
